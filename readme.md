<!-- INSTRUCTIVO BACKEND -->

<!-- Para empezar es requisito tener instalado todo lo que vamos a necesitar:

- Visual Studio Code (puede descargarse desde Microsoft Store o desde https://code.visualstudio.com/download)

- NodeJS: es la plataforma que vamos a usar para ejecutar el backend (puede descargarse desde https://nodejs.org/en)

- npm (se instala junto con NodeJS)

Pasos para ejecutar:

- Clonar repositorio del backend (https://github.com/natashaying/finalproject-backend.git) o bien abrir la carpeta adjunta en la entrega en VSCode.

- Ir a la terminal (View -> Terminal)

- Ejecutar el comando npm install (para que instale las dependencias necesarias)

- Ejecutar el comando node index.js para levantar el server (debería decir "API escuchando en http://localhost:3000")

- Abrir carpeta del frontend en una nueva ventana de VSCode y darle a "Go Live" para ver el proyecto completo en el navegador.-->
